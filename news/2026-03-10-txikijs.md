# txiki.js: 👀 tiny but surprisingly capable JavaScript runtime

**Date:** 2026-03-10  
**Category:** JavaScript / Runtime / Tooling  
**Source:** [saghul/txiki.js](https://github.com/saghul/txiki.js), [Official docs](https://txikijs.org)

## What it is

txiki.js is a 🦾**small but powerful JavaScript runtime** built with a clear goal: support modern ECMAScript and move toward **WinterTC-compatible Web API behavior**.

💁‍♀️Under the hood, it uses **QuickJS-ng** as its JavaScript engine and **libuv** as its platform layer, So basically: **small runtime footprint, but still very capable**. Tiny? yeees🫡 Weak? absolutely not🙂‍↔️

## Why it stands out

What makes txiki.js interesting is in a very fun spot between:

- a tiny embeddable runtime🧸
- a practical general-purpose JavaScript environment🌞

Kinda slay combo ngl!😍

It includes a surprisingly broad set of features:

- Web Platform APIs like `fetch`, `WebSocket`, `Console`, `setTimeout`, `Crypto`, and Web Workers (which is I'm using in my turtleneck app!!!)
- Built-in networking support including **TCP, UDP, Unix sockets** + an **HTTP server with WebSocket support**
- System-level capabilities such as **file I/O, child processes, and signal handling**
- Built-in modules like `tjs:sqlite`, `tjs:ffi`, `tjs:path`, and `tjs:hashing` highlighted in community summaries and project materials

So yeah this is not just some "look how tiny we are"😉 runtime.
It actually does stuff!

One especially cool detail: `tjs compile` can generate **standalone executables**, which makes the runtime feel more practical for small tools and distributable apps💛

## Platform support

The project documents support for major platforms including **GNU/Linux, macOS, Windows(thanks god), and other Unix-like systems** through its build and platform notes. If you are interested in,, check the link above!

## Why I think this is interesting

The JavaScript runtime space is getting more diverse.  
Node.js was the only JS runtime I knew..🐷 and Deno and Bun get most of the attention, but txiki.js? it explores a different trade-off:

**small runtime footprint + modern JS + web-style APIs + low-level system access**

That combination makes it feel less like “just another runtime” and more like a serious experiment in what a compact JavaScript environment can be. I might use this when I build my development assistant tools🙉

## My take

If you enjoy watching how JavaScript escapes the browser and keeps reinventing itself on the server, in tooling, and in standalone apps, txiki.js is worth knowing about.

It is also a nice reminder that the future of JavaScript runtimes is not only about:
**“is it faster than Node?”**

Sometimes the more interesting question is:
how **small, capable, portable and practical can a runtime get?**

Friendly summary

- size🐛
- standards compatibility🐯
- built-in APIs🐣
- system access🦜
- portability🐚

## Links

- [GitHub Repository](https://github.com/saghul/txiki.js)
- [Official Documentation](https://txikijs.org)
- [Recent Release Notes](https://github.com/saghul/txiki.js/releases)
